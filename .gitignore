# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# --- DEPENDENCIES & BUILD OUTPUT ---
# The / before the path ensures it is only ignored at that exact level relative to the .gitignore file.
# Since frontend is now a subdirectory, we target files inside it.

# Frontend dependencies
/frontend/node_modules
/frontend/.pnp
/frontend/.pnp.js

# Frontend testing
/frontend/coverage

# Frontend production build
/frontend/build

# --- MISC ---
.DS_Store

# --- ENVIRONMENT & CONFIG FILES ---
.env # This now covers the generic .env file from the Python section
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- LOG FILES ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
debug.log

# --- PYTHON (BACKEND) ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/

# --- SECRETS (CRITICAL) ---
apikey.txt
secret.txt
*.key